# Stage 1 downloads
boto3            # SpaceNet (anonymous S3)
requests         # Maxar STAC + ESA WorldCover tiles

# Stage 0-6: data pipeline
numpy
scipy
opencv-python-headless
Pillow
PyYAML
rasterio
pandas
pyarrow

# Stage 7-8: models / training
# WORKSTATION: install a CUDA-matched wheel first (see RESOLVE.md), then run
#   pip install -r requirements.txt
#   e.g.: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# GOOGLE COLAB: do NOT run this file directly. Use colab/requirements-colab.txt
#   instead — it intentionally omits torch/torchvision because Colab already
#   provides a CUDA-enabled build and pip-installing these would overwrite it.
torch
torchvision
timm

# Stage 9: evaluation + results report
scikit-image
python-docx      # results report as a shareable .docx (Markdown always written regardless)
